To use the application, download docker then run the "docker-compose build" command in the bash, shell or any terminal
once its builded, run the command "docker-compose up"
then finally run the front-end.py file by clicking the run button when its open or by first running the command "cd server" then running "python front-end.py"